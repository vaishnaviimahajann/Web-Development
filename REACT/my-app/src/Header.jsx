function Header(){
    return(
        <div>
            <h1>---HEADER AREA---</h1>
        </div>
    )
}
export default Header;

//git commit -m " learn about import stament where "We use import to bring in a component from another file. Just like you borrow a recipe from a friend — if you want to cook something, you need to get the recipe first. import means — I want to use this recipe that someone else has shared with me!"